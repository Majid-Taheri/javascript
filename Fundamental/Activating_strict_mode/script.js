'use strict';

let hasDriversLicense = false;
const passTest = false;

if (passTest) hasDriverLicense = true; //wrong variable missing 's' in hasDriverLicense
if (hasDriversLicense) console.log('I can Drive 😃');

const interface = 500; //error reserve keyword
const if = 20; //error reserve keyword